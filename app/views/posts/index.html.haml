%h1 Instagram

.posts-wrapper.row
  - @posts.each do |post|
    .post
      .post-head
        .name Gabby
      .image.center-block
        =link_to (image_tag post.image.variant(resize_to_limit: [300, 300]), class:'imgresponsive'), post_path(post)
        %p.caption
          =post.caption

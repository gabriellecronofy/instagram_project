.posts-wrapper.row
  .post
    .post-head
      .name Gabsfinnegan
    .image
      =link_to post_path(@post) do
        =image_tag @post.image.variant(resize_to_limit: [500, 500]), class: 'img-responsive center-block'
      %p.caption
        =@post.caption
      .text-center.edit-links
        =link_to "Edit Post", edit_post_path(@post)
        |
        =link_to "Back", posts_path
